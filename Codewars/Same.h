#include <vector>

#ifndef SAME_H
#define SAME_H

namespace Same {
    void do_test(std::vector<int>, std::vector<int>, bool);
    void run_tests();
    static void printVector(std::vector<int>);
}

#endif